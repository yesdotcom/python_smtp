# Basic Email Sending Program (SMTP)

A simple Python script that sends emails using the **Simple Mail Transfer Protocol (SMTP)**.

## Features

- Sends plain-text email messages
- Uses environment variables for credentials
- Compatible with most SMTP servers (e.g., Gmail, Outlook)
